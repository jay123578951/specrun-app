# specrun-app

## 走查畫面時怎麼起開發伺服器

`vite.config.ts` 把埠寫死 5173 且 `strictPort: true`——Tauri 的 `devUrl` 綁死同一個埠，埠一漂移 `pnpm dev:app` 就連不上。

**已經有一份 vite 在跑的時候，不要在這個專案另起第二份。** `node_modules/.vite` 的相依快取整個專案共用，第二份啟動時可能把它重寫，連帶讓正在跑的那份與桌面視窗拿到殘缺的樣式。vite 的 CLI 沒有改快取目錄的選項，換個埠起也隔不開，所以這條路沒有安全的走法。

順序：

1. 先看 5173 有沒有人在聽：`lsof -nP -iTCP:5173 -sTCP:LISTEN`
2. 有人在聽 → 借用那一份，不另起。擔心它是舊的就重載頁面
3. 沒人在聽 → `pnpm dev`（vite 5173 ＋ Nitro API 3210）

## 桌面視窗開起來沒有樣式時

先按 Cmd+R 重載，不要先懷疑程式碼。

UnoCSS 在開發模式分兩趟送樣式：第一趟走一般請求，只給目前產好的那些；第二趟走 HMR 的 WebSocket 補完整份。第二趟的交握包在 try/catch 裡，失敗只印一行警告，所以 App 照常掛載、資料照常讀得出來，單純少掉所有排版 class。冷啟動搶在相依快取重建完的瞬間載入就會撞到。

怎麼認出是這一種：深色背景與字體還在（那是 `src/styles/tokens.css` 給的），但寬度、間距、邊框全沒。重載即可。
