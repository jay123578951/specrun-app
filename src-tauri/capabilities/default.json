{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main 視窗權限：fs 各操作，靜態 scope 僅共用設定目錄 $CONFIG/specrun-app；專案路徑由 allow_path 於 runtime 授權；opener 外掛負責開啟外部網址，在檔案管理器中開啟位置改由自寫指令 open_in_file_manager 包該外掛的 Rust API，不經此權限清單",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-mkdir",
    "fs:allow-exists",
    "fs:allow-stat",
    "fs:allow-remove",
    "fs:allow-rename",
    "fs:allow-read-dir",
    "fs:allow-watch",
    "fs:allow-unwatch",
    "opener:allow-open-url",
    "opener:allow-default-urls",
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$CONFIG/specrun-app" },
        { "path": "$CONFIG/specrun-app/**" }
      ]
    }
  ]
}
